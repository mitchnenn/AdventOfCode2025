// Numeric operations
int sum = numbers.Sum();
int max = numbers.Max();
int min = numbers.Min();

// Grouping and counting
var groups = lines.GroupBy(x => x)
    .ToDictionary(g => g.Key, g => g.Count());

// LINQ queries
var filtered = lines.Where(x => x.Length > 5)
    .OrderBy(x => x)
    .ToList();

// Grid/2D array handling
var grid = lines.Select(l => l.ToCharArray())
    .ToArray();
char cell = grid[y][x];

